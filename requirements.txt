# Core dependencies
# Pinned exactly so rebuilds are reproducible. openai must stay at 2.44.0:
# 2.45.0 made cache_write_tokens a required field on InputTokensDetails, which
# openai-agents 0.1.0 does not pass, so constructing a Runner context raises a
# ValidationError before any API call is made.
agency-swarm[fastapi]==1.0.0b6
openai-agents==0.1.0
openai==2.44.0
python-dotenv==1.0.1

fastapi==0.129.0
uvicorn==0.41.0

# MCP Server dependencies
fastmcp==2.2.0

# Modern PDF generation
weasyprint==69.0        # Modern HTML/CSS to PDF with markdown support
markdown==3.10.2        # Full-featured markdown parser with extensions
beautifulsoup4==4.14.3  # HTML parsing and manipulation

pydantic==2.13.4
gradio==6.6.0
httpx==0.28.1
